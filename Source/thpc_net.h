#ifndef GAME_NET_H
#define GAME_NET_H
#include <SFML/Network.hpp>
#include "thpc_defines.h"
#include "thpc_debug.h"
namespace thpc
{
/*
	class PacketHandler
	{
	public:
		PacketHandler();
	private:
		
	};

	class NetClient
	{
	public:
		NetClient();
	private:
		sf::TcpSocket socket;
		sf::Packet packet;
	};

	class NetHost
	{
	public:
		NetHost();
	private:
		sf::Packet packet;
		sf::SocketSelector selector;
		sf::TcpListener listener;

		std::vector<sf::TcpSocket*> sockets;
		// change these as/when necessary for testing purposes
		const int port = 7777;
		bool listen = false;
	};
*/
}


#endif